---
description: セキュリティ関連開発のルールとベストプラクティス
globs:
alwaysApply: true
---

# セキュリティ開発ルール

テンプレートリポジトリにおけるセキュリティ機能開発のルールです。

## 基本原則

1. **Defense in Depth**: 多層防御を実装
2. **Test Before Deploy**: ローカルで必ず動作確認
3. **Document Everything**: セキュリティ実装は詳細にドキュメント化
4. **Review Required**: セキュリティ関連の変更は必ずレビュー

## セキュリティブランチでの開発

### ブランチ命名

```bash
# IPA準拠実装
security/implement-ipa-phase1
security/implement-ipa-phase2
security/implement-ipa-phase3

# 脆弱性修正
security/fix-sql-injection-rule
security/update-dependency-vulnerability

# セキュリティ機能追加
security/add-secret-scanning
security/add-dependency-check
```

### 開発フロー

```bash
# 1. セキュリティブランチ作成
git checkout -b security/implement-new-feature

# 2. 実装
# - セキュリティルール追加
# - テストケース作成
# - ドキュメント整備

# 3. ローカルでセキュリティチェック実行
./scripts/security/run-security-check.sh

# 4. テストプロジェクトで検証
cd /tmp/test-project
./scripts/security/run-security-check.sh

# 5. 問題がなければコミット
git add .
git commit -m "security: Implement new security feature with tests"

# 6. プッシュしてPR作成
git push -u origin security/implement-new-feature
```

## セキュリティルール実装時の注意事項

### Semgrep ルール追加

#### ルールファイル構造

```yaml
# scripts/security/semgrep-rules/ipa-python.yaml
rules:
  - id: ipa-new-vulnerability-check
    patterns:
      - pattern: dangerous_function($ARG)
    message: |
      危険な関数の使用を検出しました。
      IPA「安全なウェブサイトの作り方」X-(i) に準拠して修正してください。
    severity: ERROR
    languages:
      - python
    metadata:
      category: security
      ipa_section: "X-(i)"
      cwe: "CWE-XXX"
      owasp: "A03:2021"
      confidence: HIGH
```

#### 必須項目チェックリスト

- [ ] `id`: ルール識別子（`ipa-` プレフィックス）
- [ ] `message`: 日本語での分かりやすい説明
- [ ] `severity`: ERROR / WARNING / INFO
- [ ] `metadata.ipa_section`: IPA項目番号
- [ ] `metadata.cwe`: CWE番号
- [ ] `metadata.owasp`: OWASP分類

### Bandit 設定変更

```ini
# .security-template/.bandit

[bandit]
# 除外ディレクトリ追加時
exclude_dirs = ['/tests', '/new_exclude_dir']

# 新しいテスト追加時
tests = ['B201', 'B301', 'B999']  # B999 を追加

# 重大度・信頼度変更時（慎重に）
severity = MEDIUM  # HIGH に変更する場合は影響を確認
confidence = MEDIUM
```

## セキュリティチェックの動作確認

### ローカル実行テスト

```bash
# 1. セキュリティチェックスクリプト実行
./scripts/security/run-security-check.sh

# 2. 結果ファイル確認
ls -la security-results/
cat security-results/bandit-results.json
cat security-results/semgrep-python-results.json
cat security-results/semgrep-typescript-results.json

# 3. エラーがないことを確認
echo $?  # 0 なら成功
```

### テストプロジェクトでの検証

```bash
# 1. テストプロジェクト生成
cd generators
python interactive_setup.py

# 2. 生成されたプロジェクトでセキュリティチェック実行
cd /path/to/generated-project
./scripts/security/run-security-check.sh

# 3. 以下を確認
# - セキュリティファイルが正しくコピーされているか
# - ルールが正しく動作するか
# - False Positive が多すぎないか
```

### GitHub Actions 動作確認

```bash
# 1. ブランチプッシュ
git push -u origin security/new-feature

# 2. GitHub でダミーPR作成
gh pr create --title "Test: Security check workflow" \
  --body "Testing security check workflow"

# 3. Actions タブで以下を確認
# - python-security job が成功するか
# - typescript-security job が成功するか
# - security-report job が成功するか
# - PRコメントが正しく投稿されるか

# 4. 確認後、テストPRをクローズ
gh pr close <PR番号>
```

## False Positive（誤検知）対応

### 誤検知の判断基準

1. **技術的妥当性**: 実際には安全なコードか？
2. **影響範囲**: 他の箇所にも同様の誤検知があるか？
3. **ルール改善可能性**: ルールを改善して誤検知を減らせるか？

### 対応方法

#### 1. ルール改善（推奨）

```yaml
# Before: 誤検知が多い
rules:
  - id: ipa-sql-injection
    patterns:
      - pattern: execute($QUERY)  # すべてのexecute()を検出してしまう

# After: より正確な検出
rules:
  - id: ipa-sql-injection
    patterns:
      - pattern: execute($QUERY)
      - pattern-not: execute("SELECT ...")  # リテラルは除外
      - pattern-not: execute(SAFE_QUERY_CONSTANT)  # 定数は除外
```

#### 2. 除外設定追加

```ini
# .bandit
[bandit]
exclude_dirs = ['/tests', '/migrations']  # テストとマイグレーションを除外
```

```
# .semgrepignore
tests/
migrations/
**/test_*.py
**/*_test.ts
```

#### 3. インラインコメント（最終手段）

```python
# nosec B608 - この使用は安全（理由を明記）
query = f"SELECT * FROM {TABLE_NAME} WHERE id = %s"
```

## セキュリティドキュメント整備

### 必須ドキュメント

セキュリティ機能追加時は以下を必ず更新：

1. **技術スタック説明** (`.security-template/README.md`)
   ```markdown
   ## 技術スタック

   ### 新しいツール名
   - 目的: ...
   - 開発元: ...
   - 主な機能: ...
   ```

2. **動作フロー説明** (`.security-template/README.md`)
   ```markdown
   ## セキュリティチェックの動作フロー

   ### 新しいチェックの流れ
   ...
   ```

3. **実装ガイド** (`docs/security/README.md`)
   ```markdown
   ## Phase X: 新機能名

   ### 概要
   ### インストール
   ### 使い方
   ### トラブルシューティング
   ```

### ドキュメント品質チェックリスト

- [ ] 技術的正確性: 内容が正確か
- [ ] 完全性: 必要な情報がすべて含まれているか
- [ ] 明確性: 初心者でも理解できるか
- [ ] 例示: 具体例が含まれているか
- [ ] リンク: 関連ドキュメントへのリンクがあるか

## セキュリティPRのレビューポイント

### レビュアー向けチェックリスト

#### 1. セキュリティルールの妥当性
- [ ] IPAガイドラインに準拠しているか
- [ ] CWE/OWASP分類が正しいか
- [ ] False Positive が少ないか
- [ ] メッセージが分かりやすいか

#### 2. 実装品質
- [ ] ローカルでセキュリティチェックが実行できるか
- [ ] GitHub Actions が正しく動作するか
- [ ] エラーハンドリングが適切か
- [ ] パフォーマンスへの影響は許容範囲か

#### 3. テストカバレッジ
- [ ] テストプロジェクトで検証されているか
- [ ] エッジケースが考慮されているか
- [ ] 既存テンプレートへの影響がないか

#### 4. ドキュメント
- [ ] 技術スタックが説明されているか
- [ ] 使い方が明確か
- [ ] トラブルシューティングが含まれているか

### レビューコメント例

```markdown
# ✅ Good Review
> セキュリティルールの妥当性を確認しました。
>
> **確認事項**:
> - ローカルでセキュリティチェック実行 → 正常動作
> - テストプロジェクト生成 → セキュリティファイル正しくコピー
> - GitHub Actions動作確認 → PRコメント正しく投稿
>
> **改善提案**:
> - `ipa-sql-injection-string-format` ルールで `cursor.execute()` の
>   パラメータ化クエリが誤検知されています。pattern-not で除外してください。
>
> LGTM with minor fix 👍

# ❌ Bad Review
> 問題ないと思います。
```

## セキュリティインシデント対応

### 脆弱性発見時の対応フロー

```bash
# 1. 緊急度評価
# - Critical: 即座に対応（1時間以内）
# - High: 当日中に対応
# - Medium: 1週間以内に対応
# - Low: 次回メンテナンス時に対応

# 2. セキュリティブランチ作成
git checkout -b security/fix-critical-vulnerability

# 3. 修正実装
# - 脆弱性修正
# - テストケース追加
# - ドキュメント更新

# 4. ローカル検証
./scripts/security/run-security-check.sh

# 5. プッシュして緊急PR作成
git push -u origin security/fix-critical-vulnerability
gh pr create --title "Security: Fix critical vulnerability" \
  --body "🚨 Critical security fix - requires immediate review"

# 6. 即座にレビュー・マージ
```

## 禁止事項

### ❌ 絶対にやってはいけないこと

1. **セキュリティルールの無効化**
   ```bash
   # ❌ Bad
   bandit --skip B608  # ルールを無効化
   ```

2. **テストなしでのマージ**
   ```bash
   # ❌ Bad
   git commit -m "security: Add new rules"
   git push
   # ローカルでテストせずにマージ
   ```

3. **ドキュメント未整備でのリリース**
   ```bash
   # ❌ Bad
   # セキュリティ機能追加したがドキュメント未作成
   ```

4. **False Positive の無視**
   ```python
   # ❌ Bad
   # nosec  # 理由を書かずにルールを無効化
   dangerous_function(user_input)
   ```

## ベストプラクティス

### ✅ 推奨事項

1. **段階的実装**
   - Phase 1: 静的解析ツール導入
   - Phase 2: CI/CD統合
   - Phase 3: 高度な機能追加

2. **継続的改善**
   - False Positive を発見したらルール改善
   - 新しい脆弱性パターンを追加
   - ドキュメントを常に最新化

3. **コミュニティ連携**
   - IPA最新ガイドラインを確認
   - OWASP更新を追跡
   - Semgrep公式ルールを参考

## 関連ドキュメント

- [ブランチ戦略](./.cursor/rules/branch_strategy.mdc)
- [Git ワークフロー](./.cursor/rules/git_workflow.mdc)
- [セキュリティ実装ガイド](../../docs/security/README.md)
- [セキュリティテンプレート](../../.security-template/README.md)
